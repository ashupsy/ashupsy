# Core dependencies (always needed) — face detection, drawing, arrays.
# Use opencv-python on a machine with a display (webcam window);
# opencv-python-headless is fine for servers / batch image & video jobs.
opencv-python>=4.8,<5
numpy>=1.24
